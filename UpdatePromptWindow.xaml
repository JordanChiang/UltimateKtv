<Window x:Class="UltimateKtv.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="發現新版本" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True">
    <Border Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource PrimaryBrush}"
            BorderThickness="2"
            CornerRadius="10" Margin="10"
            Effect="{DynamicResource MaterialDesignShadowDepth5}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <materialDesign:PackIcon Kind="Update" Width="36" Height="36"
                                         Foreground="{DynamicResource PrimaryBrush}"
                                         VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="有可用的新版本！" FontSize="28" FontWeight="Bold"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource MaterialDesignBody}"/>
            </StackPanel>
            
            <Grid Grid.Row="1" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="0" Text="目前版本: " FontSize="20" Foreground="{DynamicResource MaterialDesignBody}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="CurrentVersionText" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource MaterialDesignBody}"/>
                
                <TextBlock Grid.Row="1" Grid.Column="0" Text="最新版本: " FontSize="20" Foreground="{DynamicResource PrimaryBrush}" Margin="0,5,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="NewVersionText" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,5,0,0"/>
            </Grid>

            <!-- Release Notes -->
            <Border Grid.Row="2" Background="{DynamicResource MaterialDesignCardBackground}"
                    CornerRadius="5" Padding="10" Margin="0,0,0,20"
                    BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ReleaseNotesText" TextWrapping="Wrap" FontSize="16" Foreground="{DynamicResource MaterialDesignBody}"/>
                </ScrollViewer>
            </Border>

            <!-- Footer -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="UpdateButton" Content="立即更新"
                        Style="{DynamicResource MaterialDesignRaisedButton}"
                        FontSize="24" Height="45"
                        Margin="5,0" Padding="20,8"
                        VerticalContentAlignment="Center"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                        Click="UpdateButton_Click"/>
                <Button Name="SkipButton" Content="稍後再說"
                        Style="{DynamicResource MaterialDesignOutlinedButton}"
                        FontSize="24" Height="45"
                        Margin="5,0" Padding="20,8"
                        VerticalContentAlignment="Center"
                        BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                        Foreground="{DynamicResource PrimaryHueMidBrush}"
                        Click="SkipButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
